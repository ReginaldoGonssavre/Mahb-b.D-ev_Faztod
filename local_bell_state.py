from qiskit import QuantumCircuitfrom qiskit.primitives import Samplerfrom qiskit.visualization import plot_histogram# Create a quantum circuit with 2 qubitsqc = QuantumCircuit(2)# Create a Bell state# Apply a Hadamard gate to the first qubitqc.h(0)# Apply a CNOT gate with the first qubit as control and second as targetqc.cx(0, 1)# Measure the qubitsqc.measure_all()# Use a local Sampler primitivesampler = Sampler()# Run the sampler jobjob = sampler.run(qc)# Get the resultresult = job.result()# Print the quasi-distributionprint("Quasi-distribution:")print(result.quasi_dists[0])# You can also get the countscounts = result.quasi_dists[0].binary_probabilities()print("\nCounts:")print(counts)# To visualize the results, you need matplotlib installed# pip install matplotlibtry:    # Plot and save the histogram    histogram = plot_histogram(counts)    histogram.savefig('bell_state_histogram.png')    print("\nA histogram of the results has been saved to bell_state_histogram.png")except ImportError:    print("\nTo visualize the results, please install matplotlib: pip install matplotlib")